<% content_for(:title, "#{@bus.name} | Bus Timetable") %>

<h1><%= @bus.name %></h1>

<h3>This bus currently runs on the <%= @bus.line.name %> line.</h3>

<%= link_to "Edit Bus", edit_bus_path, :class => "btn btn-success" %>
<%= link_to "Delete Bus", bus_path(@bus),
                              :confirm => "Are you sure?",
                              :method => "delete",
                              :class => "btn btn-danger" %>

<br />
<br />


