<% content_for(:title, "Bus Timetable") %>

<h1>Bus Timetable</h1>

<h3>Stations:</h3>
<ul>
<% @stations.each do |station| %>
  <li><%= link_to station.name, station_path(station.slug) %></li>
<% end %>
</ul>

<%= render "form" %>

<%= link_to "List of Lines", lines_path, :class => "btn btn-primary" %>
<%= link_to "List of Stops", stops_path, :class => "btn btn-primary" %>

